# Datos locales del equipo (regenerables; no publicar)
data/*.json
!data/.gitkeep

# Perfil local del equipo (opcional; no subir al repo)
config/machine.local.json

# Informes generados localmente
docs/baseline-comparison.md

# Assets fuente en alta resolucion (opcional en disco local)
assets/logo.png
assets/splash.png
assets/banner.png
img/

# Launcher VBS generado al instalar
PC Health.vbs

# Windows / editor
Thumbs.db
Desktop.ini
.vs/
.idea/
*.user
